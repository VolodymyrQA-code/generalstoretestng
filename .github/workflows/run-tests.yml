name: Android UI Tests (Local APK + Appium + Allure)

on:
  push:
  pull_request:

jobs:
  ui-tests:
    runs-on: ubuntu-latest
    timeout-minutes: 60

    steps:
      # 1️⃣ Checkout коду
      - name: Checkout repository
        uses: actions/checkout@v4

      # 2️⃣ Встановлення JDK 17
      - name: Set up JDK 17
        uses: actions/setup-java@v4
        with:
          java-version: '17'
          distribution: 'temurin'

      # 3️⃣ Кеш Maven
      - name: Cache Maven packages
        uses: actions/cache@v4
        with:
          path: ~/.m2
          key: ${{ runner.os }}-m2-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-m2

      # 4️⃣ Встановлення Node.js + Appium
      - name: Set up Node.js and Appium
        run: |
          npm install -g appium@3.0.2
          appium driver install uiautomator2
          appium driver install xcuitest
          appium --version

      # 5️⃣ Налаштування Android SDK шляхів
      - name: Set up Android SDK path
        run: |
          echo "ANDROID_HOME=$ANDROID_SDK_ROOT" >> $GITHUB_ENV
          echo "$ANDROID_HOME/emulator" >> $GITHUB_PATH
          echo "$ANDROID_HOME/platform-tools" >> $GITHUB_PATH
          echo "$ANDROID_HOME/tools" >> $GITHUB_PATH
          echo "$ANDROID_HOME/tools/bin" >> $GITHUB_PATH

      # 6️⃣ Збірка локального APK (якщо є Android проєкт)
      - name: Build local APK
        run: |
          echo "🏗️ Building APK..."
          if [ -d "app" ]; then
            cd app
            chmod +x ../gradlew || true
            ../gradlew assembleDebug
            cd ..
            cp app/build/outputs/apk/debug/app-debug.apk General-Store.apk
            echo "✅ Local APK built successfully."
          else
            echo "⚠️ No Android module found, using existing General-Store.apk"
          fi
          ls -lh General-Store.apk || echo "❌ APK file not found"

      # 7️⃣ Перевірка, що APK дійсно існує
      - name: Verify APK file
        run: |
          echo "🔍 Checking APK file..."
          if [ ! -f "General-Store.apk" ]; then
            echo "❌ APK file missing — build failed or not present."
            exit 1
          fi
          file General-Store.apk
          unzip -t General-Store.apk || echo "⚠️ Warning: APK might be corrupted."

      # 8️⃣ Запуск емулятора Android
      - name: Start Android Emulator and Run Tests
        uses: reactivecircus/android-emulator-runner@v2
        with:
          api-level: 30
          target: default
          arch: x86_64
          profile: pixel
          disable-animations: true
          script: |
            echo "🕓 Waiting for emulator to boot..."
            adb wait-for-device
            until [ "`adb shell getprop sys.boot_completed | tr -d '\r'`" = "1" ]; do sleep 5; done
            echo "✅ Emulator fully booted!"

            echo "🚀 Starting Appium..."
            appium --base-path /wd/hub > appium.log 2>&1 &
            APPIUM_PID=$!
            sleep 30

            echo "🧪 Running UI tests..."
            mvn clean test -Dsurefire.useFile=false || true

            echo "🛑 Stopping Appium..."
            kill $APPIUM_PID || true

      # 9️⃣ Генерація звіту Allure
      - name: Generate Allure report
        run: mvn allure:report

      # 🔟 Завантаження результатів як артефакт
      - name: Upload Allure report artifact
        uses: actions/upload-artifact@v4
        with:
          name: allure-report
          path: target/site/allure-maven-plugin/
